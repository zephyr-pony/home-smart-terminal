## 一、项目概述

### 项目名称

家庭智能终端（家庭大脑）

### 核心定位

一个**以记忆为核心**的家庭 AI 助手，能记录家里的一切信息（物品、事件、日程），并能基于这些信息回答问题、主动提醒、提供建议。

### 最终形态

一台 **Windows/Linux PC 作为服务终端**（家庭大脑），运行 LLM、STT、TTS、记忆存储等全部 AI 能力。家庭成员通过 **手机、平板、蓝牙音箱** 等客户端设备连接服务端，实现随处语音交互。服务端常驻运行，客户端零算力需求。

```
┌─────────────────────────────────────────────────────────────┐
│                    服务端（家庭大脑 PC）                      │
│                                                             │
│  FunASR (STT)  ·  Qwen 4B (LLM)  ·  Edge TTS (TTS)         │
│  ChromaDB (语义记忆)  ·  SQLite (结构化数据)                 │
│  FastAPI (HTTP/WebSocket 服务)                              │
│  APScheduler (定时提醒)                                     │
│                                                             │
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────────┐        │
│  │ 手机App │  │ 平板App │  │蓝牙音箱 │  │ 浏览器WebUI│        │
│  │(麦克风) │  │(麦克风) │  │(扬声器) │  │(麦克风+扬声)│       │
│  └───┬────┘  └───┬────┘  └───┬────┘  └─────┬──────┘        │
│      │           │           │              │                │
│      └───────────┴───────────┴──────────────┘                │
│                    局域网 WiFi                               │
└─────────────────────────────────────────────────────────────┘
```

### 差异化

市面上的智能音箱、Home Assistant、ChatGPT 都不能同时做到"自由记录自定义信息 + 主动提醒 + 全本地语音交互"。已有开源项目（JARVIS、Atlas Cortex、KORVIN 等）做了类似的个人 AI 助手，但**没有一个是围绕"家庭物品管理 + 保质期提醒"为核心的**，这是本项目的独特切入点。

### 功能模块

| 模块 | 功能 | 示例 |
|------|------|------|
| **语音交互** | 收音、放音、语音识别、语义理解 | "小马，明天早上八点叫我起床" |
| **记忆系统** | 将用户说的话提取关键信息，分结构化/非结构化存储 | "我今天给鼠标换了电池" → 可查询 |
| **物品管理** | 记录物品名称、位置、数量、保质期，自动更新 | 冰箱食材、日用品库存 |
| **智能提醒** | 基于时间或条件的主动提醒 | 酸奶快过期了提醒 |
| **智能推荐** | 基于现有食材推荐菜谱 | "晚上吃什么好？" |
| **视觉识别**（可选） | 摄像头识别物品自动录入 | 扫一下酸奶条形码自动录入 |

---

## 二、技术调研结论

> 以下结论基于 2025 年 7 月的公开基准测试、开源项目分析和本机硬件评估。

### 2.1 本机硬件

| 项目 | 配置 |
|------|------|
| CPU | AMD Ryzen 7 255（8核16线程，3.8GHz） |
| 内存 | 32 GB |
| GPU | AMD Radeon 780M（核显，无 NVIDIA 独显） |
| 音频 | 麦克风 + 外接音箱已就绪 |
| 系统 | Windows |

**关键约束**：无 CUDA → 无法用 GPU 加速 PyTorch 模型。但 32GB 内存充裕，CPU 推理可行；Radeon 780M 支持 Vulkan，llama.cpp 可用 Vulkan 后端获得一定加速。

### 2.2 语音识别（STT）：FunASR 替代 Whisper

原计划用 Whisper 做中文 STT，调研后发现 **FunASR 在中文场景全面碾压 Whisper**：

| 方案 | 中文 CER（越低越好） | 速度（CPU 8线程） | 模型大小 |
|------|---------------------|-------------------|---------|
| **FunASR SenseVoice** | **8.01%** | **~20× 实时** | 449 MB |
| FunASR Paraformer | 9.85% | ~21× 实时 | 401 MB |
| whisper.cpp small | 22.12% | 4.6× 实时 | 466 MB |
| whisper.cpp base | 31.33% | 9.9× 实时 | 142 MB |

同等模型大小下，FunASR 中文错误率仅为 Whisper 的 1/3，且速度更快（非自回归架构，单次前向传播）。FunASR 还内置 VAD（语音活动检测），支持情绪检测和语言识别。

**决策：STT 使用 FunASR SenseVoice**（Python SDK 或 llama.cpp 运行时）。

### 2.3 语音合成（TTS）：Edge TTS 起步

本机无 NVIDIA GPU，CosyVoice / ChatTTS 等高质量本地 TTS 在 CPU 上太慢（8-20 秒/句）。Edge TTS 虽需联网，但免费、零部署、中文质量够用（7.1/10）。

| 方案 | 中文自然度 | CPU 可行性 | 延迟 | 联网 | 阶段 |
|------|-----------|----------|------|------|------|
| **Edge TTS** | 7.1/10 | 即时 | 200-300ms | 需联网 | MVP |
| Piper | 6.0/10 | <50ms | <50ms | 离线 | 后期短句 |
| CosyVoice2 | 9.0/10 | CPU 极慢 | — | 离线 | 需 GPU |
| ChatTTS | 9.5/10 | 8-20s/句 | — | 离线 | 需 GPU |

**决策：MVP 用 Edge TTS，后期加 Piper 做短句混合调度**（参考 JARVIS 项目的 Hybrid TTS 模式：≤5 字用 Piper <50ms，长句用 Edge TTS）。

### 2.4 大模型：Qwen3.5 4B（本地已配置）

用户本机已通过 Ollama 配置 Qwen3.5 4B，先使用该模型。32GB 内存足以同时运行 LLM + STT + 向量库。

- 如果 4B 效果不够（信息提取不准确、理解能力不足），升级到 Qwen2.5 7B（Q4 量化），内存仍够用
- llama.cpp 可启用 Vulkan 后端利用 Radeon 780M 加速

**决策：MVP 用 Qwen3.5 4B，效果不好再换。**

### 2.5 记忆存储：ChromaDB + SQLite 混合架构

纯向量库不适合存储结构化数据（保质期、数量、位置），采用混合方案：

| 数据类型 | 存储方案 | 用途 | 示例 |
|----------|---------|------|------|
| 非结构化记忆 | **ChromaDB** | 语义检索，"我大概记得说过什么" | "今天给鼠标换了电池" |
| 结构化数据 | **SQLite** | 精确查询，条件过滤，过期检查 | 物品名/数量/位置/保质期 |
| 对话历史 | **SQLite** | 上下文管理 | 最近 N 轮对话 |

ChromaDB 定位是"向量数据库里的 SQLite"——`pip install` 零配置，嵌入式运行，单用户小数据量（<10万条）完全够用。

**决策：ChromaDB（语义记忆）+ SQLite（结构化数据 + 对话历史）。**

### 2.6 同类开源项目参考

| 项目 | 仓库 | 参考价值 |
|------|------|---------|
| **JARVIS** | github.com/Ambidiosidad/jarvis | 架构最接近（Ollama + Whisper + Qdrant + Piper），Docker 微服务拆分 |
| **Atlas Cortex** | github.com/Betanu701/atlas-cortex | HOT/COLD 记忆架构（ChromaDB + SQLite FTS5 + RRF 融合），直接参考 |
| **KORVIN** | github.com/nosistech/korvin | 支持 Windows 部署，LiteLLM 模型路由，记忆溢出策略 |
| **Sovyx** | github.com/sovyx-ai/sovyx | 认知记忆架构（Hebbian 学习 + 艾宾浩斯遗忘曲线） |
| **Domia** | github.com/domia-ai/domia-core | 全链路流式语音管线（LLM→TTS 逐句管道），sherpa-onnx 参考 |

---

## 三、技术选型总览

### 服务端

| 组件 | 选型 | 理由 |
|------|------|------|
| **LLM** | Qwen3.5 4B（Ollama） | 用户已配置，先跑起来，效果不好换 7B |
| **STT** | FunASR SenseVoice | 中文 CER 最低（8%），CPU 20× 实时 |
| **TTS** | Edge TTS | 免费、零部署、中文质量够用 |
| **向量库** | ChromaDB | `pip install` 嵌入式，零配置，单用户够用 |
| **结构化存储** | SQLite | 物品/保质期/对话历史，Python 内置无需安装 |
| **Web 框架** | FastAPI + Uvicorn | 原生异步，WebSocket 支持，自动生成 API 文档 |
| **定时任务** | APScheduler | 保质期检查、主动提醒 |
| **开发语言** | Python 3.11+ | 生态最全，FunASR/ChromaDB/Edge TTS 都有 Python SDK |
| **运行环境** | 本机 Windows（后期可迁移 Linux） | 32GB 内存充裕，麦克风 + 音箱已就绪 |

### 客户端

| 组件 | 选型 | 理由 |
|------|------|------|
| **Web 客户端**（MVP） | HTML + 原生 JS | 浏览器跨平台，手机/平板/电脑零安装，用 MediaRecorder API 录音 |
| **蓝牙音箱**（阶段五） | Python 脚本 + 蓝牙 A2DP | 树莓派或旧手机做蓝牙接收端 |
| **通信协议** | HTTP + WebSocket | HTTP 上传音频/文字，WebSocket 实时对话和主动推送 |
| **音频格式** | 上传 WAV/PCM 16kHz mono，返回 MP3 | 兼容浏览器播放，PCM 适配 FunASR 输入 |

---

## 四、系统架构

### 4.1 整体架构

```
┌─────────────────────── 服务端（家庭大脑 PC）───────────────────────┐
│                                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────────┐  │
│  │ STT 服务  │  │ LLM 服务  │  │ TTS 服务  │  │  记忆服务        │  │
│  │ FunASR   │  │ Qwen4B   │  │ Edge TTS  │  │ ChromaDB+SQLite │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └───────┬─────────┘  │
│       │              │              │               │            │
│       └──────────────┴──────────────┴───────────────┘            │
│                          核心编排层                                │
│                    FastAPI + WebSocket                            │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  定时任务 (APScheduler) — 保质期检查、主动提醒              │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                   │
└───────────────────────────┬───────────────────────────────────────┘
                            │ 局域网 WiFi
          ┌─────────────────┼─────────────────┐
          │                 │                 │
   ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐
   │  手机客户端   │  │  平板客户端   │  │ 蓝牙音箱客户端│
   │  · 麦克风录音 │  │  · 麦克风录音 │  │  · 扬声器播放 │
   │  · 扬声器播放 │  │  · 扬声器播放 │  │  · 按键触发   │
   │  · 文字聊天   │  │  · 文字聊天   │  │             │
   └─────────────┘  └─────────────┘  └─────────────┘
```

### 4.2 通信协议

| 通道 | 协议 | 用途 |
|------|------|------|
| 语音上传 | HTTP POST `/api/voice` | 客户端录制音频 → 上传 → 服务端 STT → LLM → TTS → 返回音频 |
| 实时对话 | WebSocket `/ws/chat` | 流式交互：客户端发音频流，服务端实时返回文字+音频流 |
| 文字聊天 | HTTP POST `/api/chat` | 不想说话时用文字输入，返回文字 + 可选 TTS 音频 |
| 主动推送 | WebSocket `/ws/notify` | 服务端定时检查到过期物品 → 向所有连接的客户端推送提醒 |
| 物品查询 | HTTP GET `/api/items` | 客户端查看物品列表（手机 App 界面展示） |
| 状态查询 | HTTP GET `/api/status` | 服务端健康检查（LLM/STT/TTS 是否就绪） |

### 4.3 客户端形态

| 客户端 | 实现方式 | 录音 | 播放 | 文字界面 | 复杂度 |
|--------|---------|------|------|---------|--------|
| **Web App**（MVP 首选） | 浏览器打开服务端地址，HTML+JS | 浏览器 MediaRecorder API | 浏览器 Audio API | 有 | 低 |
| **手机/平板** | 同 Web App，浏览器访问 | 同上 | 同上 | 有 | 零（复用 Web） |
| **蓝牙音箱** | Python 脚本 + 蓝牙音频 | 可选 USB 麦克风 | A2DP 蓝牙音频 | 无 | 中 |
| 桌面客户端 | Python GUI 或 Electron | sounddevice | sounddevice | 有 | 中 |

**MVP 策略**：先做 Web App 客户端——手机/平板/电脑浏览器打开 `http://服务端IP:8000` 即可使用，零安装。后期再考虑原生 App 或蓝牙音箱方案。

### 4.4 音频传输方案

**方案 A：整段上传（MVP，简单）**

```
客户端浏览器录音(3-10秒) → WAV/PCM 上传 → 服务端 STT → LLM → TTS → 返回 MP3
```

- 优点：实现简单，可靠
- 缺点：有上传延迟，不能打断

**方案 B：流式交互（后期，体验好）**

```
客户端实时音频流 → WebSocket → 服务端 VAD 分段 → STT → LLM 流式输出 → TTS 流式音频 → 实时播放
```

- 优点：低延迟，可打断（全双工）
- 缺点：实现复杂，需要 VAD + 流式 STT + 流式 TTS 管道

---

## 五、分阶段实施计划

### 阶段一：本地语音备忘录（MVP 核心）

**目标**：服务端核心管线跑通 — 录音 → STT → LLM → 记忆 → TTS → 播放

**范围**：仅服务端，本机麦克风/音箱直接测试，暂不做客户端

**功能**：

1. 按键触发录音（暂不实现唤醒词，降低复杂度）
2. FunASR 语音识别转文字
3. Qwen3.5 4B 提取关键信息，存入 ChromaDB
4. 语音提问 → Qwen 理解意图 → 检索 ChromaDB → 生成回答 → Edge TTS 播报

**技术栈**：

- `funasr` — FunASR SenseVoice 语音识别
- `ollama` — 调用本地 Qwen3.5 4B
- `chromadb` — 向量记忆存储
- `edge-tts` — 语音合成
- `sounddevice` — 录音
- `numpy` — 音频处理

**预估工作量**：200-300 行 Python，1-2 周

### 阶段二：服务端 API + Web 客户端

**目标**：封装 HTTP/WebSocket API，手机/平板浏览器可用

**新增功能**：

- FastAPI 封装核心管线为 HTTP API
- Web 客户端：浏览器录音 → 上传 → 获取回答音频 → 播放
- 文字聊天界面（不想说话时打字）
- 服务端常驻运行（不再是脚本式跑完就退出）

**新增技术**：

- `fastapi` + `uvicorn` — HTTP/WebSocket 服务
- `python-multipart` — 音频文件上传
- Web 前端：HTML + JS（MediaRecorder API 录音，Audio API 播放）

**预估工作量**：2 周

### 阶段三：物品管理 + 主动提醒

**目标**：结构化物品记录，保质期自动提醒

**新增功能**：

- LLM 从对话中提取结构化物品信息（名称、数量、位置、保质期）→ 存入 SQLite
- 定时任务扫描 SQLite，检查即将过期物品
- 用户说"吃了两个酸奶" → LLM 解析 → SQLite 自动扣减数量
- 主动语音提醒通过 WebSocket 推送到所有在线客户端
- Web 客户端展示物品列表界面

**新增技术**：

- SQLite — 物品表（id, name, quantity, location, expiry_date, created_at, updated_at）
- `apscheduler` — 定时任务（每小时检查一次保质期）
- Qwen function calling — 结构化信息提取

**数据流**：

```
用户说话/打字 → FunASR/直接文本 → Qwen 判断意图
  ├─ 记忆类 → ChromaDB
  ├─ 物品类 → 提取结构化数据 → SQLite
  └─ 查询类 → 检索 ChromaDB + SQLite → Qwen 生成回答 → TTS → 返回客户端
```

**预估工作量**：2-3 周

### 阶段四：智能推荐

**目标**：基于现有食材推荐菜谱

**新增功能**：

- 查询 SQLite 中现有食材
- Qwen 基于食材生成菜谱推荐
- 考虑保质期优先消耗即将过期的食材
- 语音播报推荐

**预估工作量**：1-2 周

### 阶段五：唤醒词 + 持续监听 + 蓝牙音箱

**目标**：解放双手，始终在线，蓝牙音箱可用

**新增功能**：

- 唤醒词检测（openWakeWord 或 sherpa-onnx keyword spotting）
- VAD（语音活动检测）自动判断说话结束
- 全双工交互（可打断正在播放的回答）
- 蓝牙音箱客户端：Python 脚本连接蓝牙音箱播放 TTS 音频

**预估工作量**：2-3 周

### 阶段六：视觉识别（可选）

**目标**：摄像头/扫码自动录入物品

**新增功能**：

- 条形码扫描 → 查询商品信息 → 自动录入
- 摄像头拍照 → 本地视觉模型识别物品

**预估工作量**：2-3 周

---

## 六、MVP 数据流详解

### 阶段一：本地运行（脚本模式）

录音 → 存储和查询全在本机完成，用键盘按键触发。

```
1. 用户按空格键开始录音 → sounddevice 录制 PCM（16kHz, mono）
2. FunASR SenseVoice 识别中文文本
3. Qwen3.5 4B 分析文本，提取：
   - 关键信息摘要（存 ChromaDB，带 embedding）
   - 原文 + 时间戳（存 ChromaDB metadata）
4. 确认存储 → Edge TTS 播报"记住了" → 音箱播放
```

查询流程：

```
1. 用户按空格键开始录音 → FunASR 识别问题
2. Qwen 分析问题，提取检索关键词
3. ChromaDB 语义检索 top-5 相关记忆
4. 检索结果 + 原问题送入 Qwen → 生成自然语言回答
5. Edge TTS 合成语音 → 音箱播放
```

### 阶段二：C/S 模式（Web 客户端）

录音在客户端浏览器完成，通过 HTTP 上传到服务端处理。

**语音交互流程**：

```
客户端（浏览器）                          服务端（PC）
─────────────────                       ──────────────────
1. 按钮触发录音
   MediaRecorder API
   录制 3-10s 音频
        │
        │  POST /api/voice (WAV)
        ▼
                                    2. FunASR STT → 文本
                                    3. Qwen 判断意图
                                       ├─ 记忆存储 → ChromaDB
                                       └─ 查询 → 检索 ChromaDB
                                    4. Qwen 生成回答文本
                                    5. Edge TTS → MP3 音频
        │
        │  返回 JSON {text, audio_url}
        ▼
6. 播放回答音频
   Audio API 播放 MP3
7. 同时显示文字回答
```

**主动提醒流程**：

```
服务端                                  客户端（浏览器）
─────────────────                       ──────────────────
1. APScheduler 每小时触发
2. 查询 SQLite 即将过期物品
3. 生成提醒文本 → Edge TTS → MP3
        │
        │  WebSocket /ws/notify
        │  {type: "reminder", text, audio_url}
        ▼
                                    4. 弹出通知 + 自动播放提醒音频
```

---

## 七、本机开发环境准备清单

### 阶段一：本地脚本模式

```bash
# Python 环境
python -m venv .venv
.venv\Scripts\activate

# 核心依赖
pip install funasr           # FunASR STT
pip install ollama           # Ollama Python client
pip install chromadb         # 向量数据库
pip install edge-tts         # TTS
pip install sounddevice      # 录音
pip install numpy            # 音频处理

# 确保 Ollama 已安装且 Qwen3.5 4B 已拉取
ollama list                  # 确认模型存在
```

### 阶段二：服务端 API + Web 客户端

```bash
# 追加依赖
pip install fastapi          # Web 框架
pip install uvicorn          # ASGI 服务器
pip install python-multipart # 音频文件上传

# 服务端启动
uvicorn main:app --host 0.0.0.0 --port 8000

# 客户端访问
# 手机/平板/电脑浏览器打开 http://服务端IP:8000
```

### 阶段三：物品管理 + 定时提醒

```bash
# 追加依赖
pip install apscheduler      # 定时任务
# SQLite 无需安装，Python 内置 sqlite3 模块
```

---

## 八、风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| Qwen3.5 4B 信息提取能力不足 | 记忆存储不准确 | 换 Qwen2.5 7B Q4 量化，32GB 内存够用 |
| Edge TTS 联网不可用 | 无法语音播报 | 加 Piper 作为离线降级方案 |
| FunASR 在 Windows 安装困难 | STT 不可用 | 用 faster-whisper 降级（中文 CER 高但能跑） |
| ChromaDB 元数据过滤能力弱 | 物品查询不便 | 结构化数据全部走 SQLite，ChromaDB 仅做语义检索 |
| AMD APU 无 CUDA | TTS/LLM 无 GPU 加速 | CPU 推理可接受；llama.cpp Vulkan 后端可选加速 |
| 浏览器麦克风权限受限 | Web 客户端无法录音 | 需 HTTPS 或 localhost；局域网可用自签证书 |
| 服务端 PC 不在线 | 客户端无法使用 | 后期可考虑轻量 Linux 服务器常驻运行 |
| 多客户端同时请求 | LLM 推理排队 | MVP 单用户场景可忽略；后期加请求队列 |
